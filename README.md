# WorkMarketExample

The three files require a web server if that wasn't obvious enough, in order for python to work it needs a python web server like flask or python. Contact me for more information.
